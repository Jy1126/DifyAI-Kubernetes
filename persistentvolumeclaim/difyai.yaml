apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: difyaiv1142
  name: difyai-k8s
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
