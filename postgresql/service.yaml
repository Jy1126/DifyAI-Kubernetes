apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: difyai-1.11.4
spec:
  selector:
    app: postgres
  type: NodePort
  ports:
    - name: postgres-port
      protocol: TCP
      targetPort: 5432
      port: 5432
